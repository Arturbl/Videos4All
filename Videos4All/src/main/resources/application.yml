spring:
  application:
    name: Videos4All
  datasource:
    url: jdbc:postgresql://localhost:5432/Videos4All
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false
app:
  jwt:
    secret: secret

